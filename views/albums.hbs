{{#each songs}}
    <div>
        <div>
            <img src={{this.images.0.url}} alt="">
        </div>
        <div>
            <h3>{{this.name}}</h3>
            <a href="/tracks/{{this.id}}" >View Tracks</a>
        </div>
    </div>
{{/each}}